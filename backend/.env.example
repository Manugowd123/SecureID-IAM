DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=secureid_db
# Optional: max MySQL connections held per running instance. Keep this low
# (default 3) on serverless platforms (e.g. Vercel), since each warm
# function instance opens its own pool against the same external database.
DB_CONNECTION_LIMIT=3

PORT=3000
NODE_ENV=development

EMAIL_USER=manojgowda42666@gmail.com
EMAIL_APP_PASSWORD= xmyj ymqz vkbf upmj

JWT_SECRET=b6da8fd984ccfec0a627bbf2bc60ec3b806a806310ff4c73e392059e3fc19c546f4fb2af63ac5affe51dbf2c2a269697986bca7c60132bdb4d0e7fba90bbe15e
JWT_EXPIRES_IN=15m

# Optional: OpenID Connect issuer URL used in OIDC id_token claims.
# Defaults to http://localhost:3000 for local development; set this to
# your real production URL (e.g. https://your-app.vercel.app) in production.
OIDC_ISSUER=http://localhost:3000
